export const GAME_WIDTH = 960
export const GAME_HEIGHT = 640

/** Tank forward speed in pixels per second. */
export const TANK_SPEED = 200

/** Reverse speed is slower than forward, as a fraction of TANK_SPEED. */
export const TANK_REVERSE_FACTOR = 0.5

/** Tank rotation speed in radians per second. */
export const TANK_ROTATION_SPEED = 3
